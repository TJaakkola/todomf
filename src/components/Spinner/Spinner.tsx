import './Spinner.css'; // Import the CSS styles

const Spinner = () => {
  return (
    <div id="main-spinner-container">
      <div id="main-spinner"></div>
    </div>
  );
};

export default Spinner;